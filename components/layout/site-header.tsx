import { HeroHeaderNav } from "@/components/sections/hero/hero-header";

export function SiteHeader() {
  return (
    <header className="site-header" role="banner">
      <HeroHeaderNav />
    </header>
  );
}
